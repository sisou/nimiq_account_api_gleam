meta {
  name: send/exchange/{{amount}}
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/api/send/exchange/1500000000
  body: none
  auth: bearer
}

auth:bearer {
  token: {{API_KEY}}
}

settings {
  encodeUrl: true
}
